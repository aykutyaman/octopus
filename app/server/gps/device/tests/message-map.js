const { describe, it } = global;
import {expect} from 'chai';
import {stub, spy} from 'sinon';
import { messageMap } from '../message-map';


describe('server.gps.device.message-bus', () => {
  it('should call mapper with gps data', () => {

  });
});
